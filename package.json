{
  "name": "intelliwealth",
  "version": "0.1.0",
  "description": "Multi-agent AI wealth advisory system with deterministic constraint enforcement.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start:api": "tsx src/api/server.ts",
    "test": "node --import tsx --test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "dotenv": "^17.2.2",
    "fastify": "^5.6.1"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  }
}
